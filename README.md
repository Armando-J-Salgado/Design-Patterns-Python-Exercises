# Design Patterns Exercises

Python exercises to practice identifying design smells, SOLID violations, and refactoring toward cleaner object-oriented designs.

## Structure

Each exercise lives under `exercises/` and contains:

- `legacy/`: functional but poorly designed code
- `tests/`: pytest suites that must pass before and after refactoring

## How to run

Install dependencies and run the tests with pytest.
